name: Refresh overstock mitigation data

on:
  schedule:
    - cron: "45 * * * *"
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/refresh-overstock-mitigation.yml"
      - "dashboards/overstock-mitigation/scripts/build_data.py"
      - "dashboards/overstock/scripts/build_data.py"

permissions:
  contents: write

concurrency:
  group: refresh-overstock-mitigation
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install deps
        run: pip install --quiet google-api-python-client google-auth

      - name: Build data.json
        env:
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
        run: |
          python3 dashboards/overstock-mitigation/scripts/build_data.py \
            dashboards/overstock-mitigation/data.json

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dashboards/overstock-mitigation/data.json
          if git diff --cached --quiet -- dashboards/overstock-mitigation/data.json; then
            echo "No data changes."
            exit 0
          fi
          git commit -m "chore: refresh overstock mitigation data ($(date -u +%Y-%m-%dT%H:%MZ))"
          git push
