name: Refresh market penetration data

on:
  schedule:
    - cron: "40 * * * *"
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/refresh-market-penetration.yml"
      - "dashboards/market-penetration/scripts/build_data.py"

permissions:
  contents: write

concurrency:
  group: refresh-market-penetration
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install deps
        run: pip install --quiet google-api-python-client google-auth

      - name: Build penetration data
        env:
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
        run: |
          python3 dashboards/market-penetration/scripts/build_data.py \
            dashboards/market-penetration/data.json

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dashboards/market-penetration
          if git diff --cached --quiet -- dashboards/market-penetration; then
            echo "No data changes."
            exit 0
          fi
          git commit -m "chore: refresh market penetration data ($(date -u +%Y-%m-%dT%H:%MZ))"
          git push
