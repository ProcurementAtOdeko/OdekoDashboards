name: Refresh sales tracker data

on:
  schedule:
    - cron: "25 * * * *"
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/refresh-sales-tracker.yml"
      - "dashboards/sales-tracker/scripts/build_data.py"

permissions:
  contents: write

concurrency:
  group: refresh-sales-tracker
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install deps
        run: pip install --quiet google-api-python-client google-auth

      - name: Build per-warehouse data files
        env:
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
        run: |
          python3 dashboards/sales-tracker/scripts/build_data.py \
            dashboards/sales-tracker/data

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dashboards/sales-tracker/data
          if git diff --cached --quiet -- dashboards/sales-tracker/data; then
            echo "No data changes."
            exit 0
          fi
          git commit -m "chore: refresh sales tracker data ($(date -u +%Y-%m-%dT%H:%MZ))"
          git push
