# OdekoDashboards

Static dashboards built from Google Sheets data. Each dashboard is a folder
under `dashboards/`, refreshed hourly by a GitHub Actions workflow.

## Layout

```
.mcp.json                              # registers google-sheets MCP
tools/google-sheets-mcp/               # MCP server (Python, service-account auth)
.github/workflows/refresh-<name>.yml   # one per dashboard
dashboards/_shared/odeko-logo.png      # shared logo, referenced as ../_shared/odeko-logo.png
dashboards/<name>/
  index.html                           # Chart.js + vanilla JS, single file
  data.json                            # generated by workflow, committed
  scripts/build_data.py                # pulls sheet, aggregates, writes data.json
```

## Default data source

Unless the user names a different sheet/folder, assume new dashboards are
built from files in the **Looker Data Dumps** Google Drive folder:

- Folder name: `Looker Data Dumps`
- Folder ID: `1kpM0QOi7Wriuk_Xf6uYYR9a6RqMyBCT7`
- URL: https://drive.google.com/drive/folders/1kpM0QOi7Wriuk_Xf6uYYR9a6RqMyBCT7
- Owner: bhiney@odeko.com (already shared with the service account)

Files in this folder are Looker scheduled exports — Google Sheets with
`.csv` in the title. They refresh on Looker's schedule, so dashboards
just need to re-read the latest values.

To browse it, use the Drive MCP `search_files` with
`parentId = '1kpM0QOi7Wriuk_Xf6uYYR9a6RqMyBCT7'`. To filter by name, add
`and title contains '<keyword>'`.

## New dashboard intake

When the user asks for a new dashboard, before writing any code:

1. **Find the source file.** Search the Looker Data Dumps folder by
   keyword from the request. If multiple candidates match (e.g. several
   "Stock Report" files), list them and ask which one — prefer the
   most-recently-modified unless the user says otherwise.
2. **Inspect the sheet.** Use `get_spreadsheet_info` + `read_range` on
   `A1:Z6` of the relevant tab to confirm columns.
3. **Ask the user (batched in a single `AskUserQuestion` call):**
   - **Purpose / audience** — what decision does this dashboard support?
   - **Key metrics / views** — KPI cards? Charts? Tables? Which columns?
   - **Filters / groupings** — by warehouse, item, date range, etc.?
   - **Refresh cadence** — hourly (default) or different?
   Skip any question the user has already answered in their request.
4. **Confirm the dashboard slug** (folder name under `dashboards/`)
   before scaffolding files.

## Adding a new dashboard

Model new dashboards on `dashboards/dca1-onhand-eta/`. Steps:

1. Use the `google-sheets` MCP to inspect the source sheet:
   - `get_spreadsheet_info(url)` for tab list
   - `read_range(url, "Tab!A1:Z6")` to see column names and sample data
2. Create `dashboards/<name>/scripts/build_data.py`:
   - Parameterize `SPREADSHEET_ID`, `SHEET_RANGE`, filter values at the top
   - Read `GOOGLE_SERVICE_ACCOUNT_JSON` env var for auth
   - Aggregate by item; output a JSON shaped for the dashboard
3. Create `dashboards/<name>/index.html` (copy DCA1's, adjust charts/columns). Reference the logo as `../_shared/odeko-logo.png`.
4. Create `.github/workflows/refresh-<name>.yml` (copy DCA1's, update name, concurrency group, paths). The workflow should write `dashboards/<name>/data.json`.
5. Confirm the source sheet is shared with `sheets-mcp-bot@sheets-mcp-497414.iam.gserviceaccount.com`
6. Add the new refresh workflow's `name:` to the `workflow_run.workflows` list in `.github/workflows/pages.yml`, so hourly data refreshes redeploy the live site.
7. Commit + push to main. The push triggers the workflow, which writes `data.json` and commits.

## Live URLs

Dashboards are deployed to GitHub Pages at
`https://procurementatodeko.github.io/OdekoDashboards/<name>/`.
**Always give the user the live URL whenever something new is built.**

## Workflow conventions

- `git add <file>` BEFORE `git diff --cached --quiet` so new files get detected
- Commit author: `github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>`
- Workflow `concurrency.group` is unique per dashboard so they don't block each other

## Secrets / auth

- Service account email: `sheets-mcp-bot@sheets-mcp-497414.iam.gserviceaccount.com`
- Repo secret: `GOOGLE_SERVICE_ACCOUNT_JSON` (already configured) — used by workflows
- Env var: `GOOGLE_SERVICE_ACCOUNT_JSON` (set in Claude Code on the web env) — used by the MCP
- Both hold the same minified service-account JSON key

## Viewing dashboards locally

Browsers block `fetch()` on `file://` URLs, so use a local HTTP server.
Serve from the `dashboards/` root so each dashboard can resolve the shared
logo at `../_shared/odeko-logo.png`:
```
cd dashboards && python3 -m http.server 8765
```
Then open http://localhost:8765/<name>/.

## Visual style (Odeko brand)

All dashboards must visually match `dashboards/dca1-onhand-eta/index.html`.
Use that file as the design reference for new dashboards.

**Palette (CSS variables, defined in `:root`):**
- `--bg: #FBF7EC` — warm cream background
- `--panel: #FFFFFF` — card surface
- `--line: #E6DFCC` — warm hairline borders
- `--ink: #2A1408` — body text, deep coffee
- `--ink-2: #40180B` — headings, Odeko brand brown
- `--muted: #8A7B6A` — warm gray
- `--accent-yellow: #FFD100` — sparing accent (KPI left border, primary button)
- Status: `--green: #1F7A33`, `--amber: #B5660A`, `--red: #B42318`

**Conventions:**
- Font: Inter (with system fallbacks)
- Sticky header at top with `../_shared/odeko-logo.png` and h1 title
- KPI cards in responsive grid; values use `font-variant-numeric: tabular-nums`
- Section cards: white panel, 1px `--line` border, 8px radius, uppercase letter-spaced h3 header
- Chart.js charts: set `Chart.defaults.color = "#8A7B6A"`, `borderColor = "#E6DFCC"`, font family Inter
- Status colors in charts use the palette red / amber / green
- The Odeko logo lives at `dashboards/_shared/odeko-logo.png` and is referenced as `../_shared/odeko-logo.png` from each dashboard's `index.html`
